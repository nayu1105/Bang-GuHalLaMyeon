<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycom.myapp.city.dao.CityDao">
	<select id="cityList" resultType="com.mycom.myapp.city.dto.CityCodeDto">
		select code sidoCode, name sidoName
		from sido_code
	</select>
	
	<select id="gugunList" parameterType="map" resultType="com.mycom.myapp.city.dto.CityCodeDto">
		select code gugunCode, name gugunName
		from gugun_code
		where sido_code = #{sidoCode}
	</select>
	
	<select id="dongList" parameterType="map" resultType="com.mycom.myapp.city.dto.CityCodeDto">
		SELECT dongCode, dongName FROM my_house.dong_code
		where sidoName = #{sidoName} and gugunName= #{gugunName} and dongName is not null;
	</select>
	
	<select id="lawdcdList" parameterType="map" resultType="com.mycom.myapp.city.dto.CityCodeDto">
		select code lawdcd, name gugunName
		from gugun_code
	</select>
	
</mapper>
